# 工程名称
workspace "SHTableView.xcworkspace"
# App工程名称
xcodeproj "SHTableView.xcodeproj"

# 最低要求版本
platform :ios, "9.0"
# 去除pod库警告
#inhibit_all_warnings!

# use_frameworks!

# pod库只存在当前目标
# pod库只存在当前目标
target :"SHTableView" do

pod 'MJRefresh','3.7.5'
end

